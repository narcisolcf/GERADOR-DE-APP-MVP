{
  "name": "lean-architect-engine",
  "private": true,
  "version": "1.0.0",
  "description": "A modular architecture simulation tool powered by Gemini.",
  "scripts": {
    "start": "serve -s . -l ${PORT:-8080}"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
